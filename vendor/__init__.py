# Vendored third-party code. See each subdirectory's LICENSE and NOTICE
# at the repo root for provenance. Nothing here is installed; the modules
# are imported as plain files on the repo's import path (vendor.<name>).
